[package]
name = "macro-atlas"
version = "0.2.0"
description = "Offline macroeconomic research atlas"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
atlas-research-store = { path = "../research-store" }
serde_json = "1"

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]

[profile.release]
strip = true
opt-level = "s"
lto = "thin"
codegen-units = 1

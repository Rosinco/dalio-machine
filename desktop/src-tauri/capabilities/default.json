{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Local desktop window. No remote hosts or arbitrary filesystem access.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
